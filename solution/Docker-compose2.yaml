version: '3'
services:
  web:
    # existing configuration for the web service...

  db:
    # existing configuration for the db service...

  prometheus:
    image: prom/prometheus:v2.22.0
    ports:
     - "9090:9090"
    volumes:
     - ./prometheus.yml:/etc/prometheus/prometheus.yml
